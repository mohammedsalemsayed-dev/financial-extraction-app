"""
financial_extract — local PDF -> Excel financial-table extraction app.

A localhost-only UI wrapper around the deterministic geometry engine in
telecom_extract.py.  No cloud AI, no local LLM, no OCR, no network access
for document content.
"""
__version__ = "0.1.0"
